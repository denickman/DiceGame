
runApp() - provided by Flutter SDK. requires a widget-params (a combination of widgets)
runApp() → функция, которая запускает виджетовое дерево и «прикрепляет» его к экрану.

  MaterialApp - start point / core widget
  MaterialApp → корневой виджет, который задаёт стиль, тему, навигацию и главный экран приложения.

  MaterialApp — это обёртка, которая:
  задаёт корневой контекст для Material Design-виджетов;
  управляет навигацией и маршрутами;
  задаёт тему, локализацию и другие настройки.
  Но сам по себе MaterialApp — пустой контейнер

  У MaterialApp есть несколько способов определить, какой виджет отобразить первым:
  Через home: (самый простой способ):
  Через routes: и initialRoute::
  Через onGenerateRoute: (гибкая логика навигации):




const - a key provided by Dart, to help Dart optimize runtime performance of the app

const (от constant) означает:
значение вычисляется один раз на этапе компиляции (compile-time constant),
и дальше это значение никогда не меняется.

То есть объект, помеченный const, создаётся только один раз, и Dart может переиспользовать его, 
вместо того чтобы каждый раз заново выделять память.

Flutter строит UI из кучи маленьких виджетов (дерево виджетов).
Когда что-то меняется (например, setState), Flutter перестраивает часть дерева.
Если виджет создан с const, то Flutter понимает, что он неизменяемый и одинаковый всегда.
Такой виджет не будет пересоздаваться заново, а будет переиспользован → это экономит память и ускоряет перерисовку.




Scaffold — это каркас экрана (шаблон страницы) во Flutter.
Он предоставляет готовую структуру для типичного приложения в стиле Material Design:
верхняя панель (AppBar),
основное содержимое (body),
плавающая кнопка (floatingActionButton),
нижняя панель (bottomNavigationBar),
боковое меню (drawer),
и другие элементы.
Проще говоря, Scaffold = «скелет» для страницы, чтобы не собирать всё вручную.




🔹 Сравнение

Характеристика	            MaterialApp	            Scaffold

Уровень	                    Всё приложение	        Один экран

Отвечает за	                Тему, маршруты,         Макет страницы (AppBar, FAB, Drawer и т.д.)
                            локализацию	            

Где используется	        Корень runApp	        Внутри MaterialApp

Количество на проект	    Обычно 1	            Сколько экранов, столько Scaffold

Аналог в iOS (условно)	    UIApplication	        UIViewController


Container() 
- просто коробка для одного ребёнка (child), где ты задаёшь размеры сам (width, height, constraints).
 
❌ Ошибка: нельзя пометить весь Scaffold (и всё дерево) как const, потому что:
Container() не является compile-time константой (у него нет const конструктора).
Center() и Scaffold() — имеют конструкторы const, их можно сделать константными.
Text('...') тоже можно сделать const, потому что строка '...' — compile-time константа.



1. Иерархия виджетов (MaterialApp → Scaffold → Container → Center → Text)
MaterialApp: Задаёт общую структуру приложения, включая тему, навигацию и поддержку Material Design. 
Это как "корень" приложения.

Scaffold: Предоставляет базовую структуру экрана (например, место для AppBar, Body, FloatingActionButton и т.д.).

Container: Универсальный виджет, который используется для размещения содержимого, задания отступов, 
размеров, фона и других свойств.

Center: Центрирует свой дочерний элемент (в данном случае Text) по горизонтали и вертикали.

Text: Отображает текст на экране.








2. Вспомогательные классы (BoxDecoration, LinearGradient, TextStyle)

📌 Разница между Widget и объектами-конфигурациями
Widget → часть дерева UI, Flutter их сравнивает и решает, что перерисовать.
Config object → вспомогательные классы, которые передаются внутрь виджетов, чтобы задать их внешний вид или поведение.
Эти объекты сами по себе ничего не рисуют, пока их не использует виджет.


Эти классы не являются виджетами, а используются для настройки внешнего вида виджетов. 
Они задают стили, такие как цвета, градиенты, шрифты и другие визуальные свойства:

BoxDecoration: 
Определяет оформление контейнера, например, фон, границы, тени или градиенты. 
В твоём коде он используется для задания градиентного фона Container.

LinearGradient: Класс, который задаёт линейный градиент (переход между цветами). 
Он используется внутри BoxDecoration для создания плавного цветового перехода.

TextStyle: Определяет стиль текста, такой как цвет, размер шрифта, толщина и т.д. 
В твоём коде он применяется к Text для настройки цвета и размера текста.



class GradientContainer extends StatelessWidget 

Параметр BuildContext context в методе build — это объект, 
который предоставляет информацию о положении виджета в дереве виджетов Flutter. Он нужен для:

Доступа к данным о родительских виджетах (например, тема, размеры экрана).
Навигации, получения зависимостей или взаимодействия с другими частями приложения.
Определения, где в дереве находится текущий виджет.

Коротко: context — это "паспорт" виджета, связывающий его с деревом и окружением Flutter.